# NOTE 
# MAKE A SHARED LIBRARY by This CMake.

# 1. sub directory makes own object library
# 2. from this context, check the dependency -> target include directory
# 3. public header should be set as target property.
# 4. Get all object library list after [#1]

